<h1> <%= t('login.title')%> </h1>

<p>
	<%= t('login.description') %>
</p>

<%= form_tag login_path do %>
	<p>
		<%= label_tag :email, t('labels.login.email') %>
		<%= text_field_tag :email %>
	</p>
	<p>
		<%= label_tag :password, t('labels.login.password') %>
		<%= password_field_tag :password %>
	</p>
	<p>
		<%= submit_tag t('helpers.submit.login') %>
	</p>
<% end %>
